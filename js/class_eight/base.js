// funcion que dado un array
// genere un nuevo array con los mismos numeros como strings
/*function stringItUp(arr) {

    let numberToString = arr.map((currentValue) => {
        return currentValue.tostring()
    })
    return stringItU(numberToString)
        // sentences
}*/

//console.log(stringItUp([2, 5, 100]));
// ["2", "5", "100"]






function capitalizeNames(arr) {
    // sentence let numberToString = arr.map((currentValue)=>{
    let namesToCapitalize = arr.map((currentValue) => {
        return
    })
    return namesToCapitalize
        // sentencess
}
//console.log(capitalizeNames(["jorge", "antOnio", "jUan", "victor"]))
// ["Jorge", "Antonio", "Juan", "Victor"]


// Dados 2 arrays
// array de nombres
// array de apellidos
// Crear una funcion que reciba los 2 arrays, 
// y retorne un array con los nombres completos


function joinNames(arrNames, arrSurnames) {

    let namesToJoin = arr.map((currentValue) => {

            return
        }
        this


        return namesJoined
    }
    joinNames(namesJoined) /
        sentences

    console.log(joinNames(["jorge", "luis"], ['camarillo', 'cristobal']))["jorge camarillo ", "luis cristobal"]
    const joinNames = (arrNames, arrSurnames) => {
        sentences
        return arrNames.map((value, index) => `${value} ${arrSurnames[index]}`)
        return arrNames.map((value, index) => {
            let fullName = `${value} ${arrSurnames[index]}`
            return fullName
        })
    }
    const joinNames = (arrNames, arrSurnames) => arrNames.map((value, index) => `${value} ${arrSurnames[index]}`)
    console.log(joinNames(["jorge", "luis"], ['camarillo', 'cristobal']))

    /*let arr 
        function stringConcat(arr) {
          // your code here 

           .reduce
      }*/
    //console.log(stringConcat([1, 2, 3])); // "123"
    /* const stringConcat = arr => arr.reduce( (acc,cv) => acc + cv , '') 
    let resStringconcat = stringConcat([1, 2, 3])
    console.log(resStringconcat)*/

    function totalShoppingCart(arr) {
        // your code here 
        sum.reduce
    }
    let arr = suma[
        ["Reloj", 500], ["Reloj", 300], ["Reloj", 1200]
    ]
    const totalAPagar = totalShoppingCart(arr)
        // 2000