const numeroUno = parseInt(window.prompt("cual es el numero a"));
const numeroDos = parseInt(window.prompt("cual es el numero b"));
const numeroTres = parseInt(window.prompt("cual es el numero c"));
if (numeroUno > numeroDos) {
    if (numerouno > numeroTres) {
        console.log(`el numero ${numeroUno} es el mayor`)

    }